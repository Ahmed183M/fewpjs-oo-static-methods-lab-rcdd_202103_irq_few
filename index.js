class Formatter {
  static capitalize(string){
    console.log()
  }
  
  static sanitize(string){
    string[0]=string[0].toUpperCase();
  }
  
  static titleize(string){
    string[0]=string[0].toUpperCase();
  }
}